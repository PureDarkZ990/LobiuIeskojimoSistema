<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lobiupaieskossistema">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LobiuPaieskosSistema"
        tools:targetApi="31">

        <activity android:name=".LogInActivity"
            android:exported="true">
        </activity>

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity"
            android:exported="true">
        </activity>

        <activity android:name=".ProfileActivity"
            android:exported="true">
        </activity>

        <activity android:name=".GroupsActivity"
            android:exported="true">
        </activity>
        <activity android:name=".NewGroupActivity"
            android:exported="true">
        </activity>

        <activity android:name=".caches.CacheAddActivity"
            android:exported="true" />
        <activity android:name=".caches.ManageCacheActivity"
            android:exported="true" />
        <activity android:name=".caches.ChangeZoneActivity" android:exported="true" />
        <activity android:name=".caches.EditCacheActivity" android:exported="true" />
        <activity android:name=".caches.ManageThemeActivity" android:exported="true" />

        <activity android:name=".FriendshipActivity"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCPPmaP3CMP-5tX77_17wN96YAqTY5Ultc" />
    </application>

</manifest>
